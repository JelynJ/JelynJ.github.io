document.addEventListener("DOMContentLoaded", function() {
    const images = document.querySelectorAll('.lib_content img, .lib2_content2 img');
    images.forEach(img => img.classList.add('show'));
});
